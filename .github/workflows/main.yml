name: first-workflow

on:
  push: 
    branches: 
      - main

jobs:
  ex-job:
    runs-on: ubuntu-latest

    steps:
      - name: print welcome message
        run: echo "welcom to our first github action workflow!"

    
      
